import type { Locales, Formatters } from './i18n-types';

function initFormatters(locale: Locales): Formatters {
  return {
    // add your formatter functions here
  };
}

export { initFormatters };
